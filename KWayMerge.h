#include "PSRS.h"

void printVector(vector<vector<int>> &inputVector);
void deleteElement(vector<vector<int>> &inputVector, int vectorIndex);
vector<int> merge(vector<vector<int>> poVector); // poVector will be the vector
                                                 // of partially ordered vectors
